// TODO: Define validation rules for lead creation and updating

const createLeadSchema = {
  // TODO: Add fields e.g., name, email, phone, status, source
};

const updateLeadSchema = {
  // TODO: Add fields e.g., name, email, phone, status, source
};

module.exports = {
  createLeadSchema,
  updateLeadSchema,
};
